{reactions}
	<p class="reactions">
		{total_count:} <reaction><span>{>icon/reaction}</span><span>{total_count}</span></reaction>
		{+1:} <reaction><span>👍</span><span>{+1}</span></reaction>
		{-1:} <reaction><span>👎</span><span>{-1}</span></reaction>
		{hooray:} <reaction><span>🎉</span><span>{hooray}</span></reaction>
		{confused:} <reaction><span>😐</span><span>{confused}</span></reaction>
		{laugh:} <reaction><span>🤣</span><span>{laugh}</span></reaction>
		{heart:} <reaction><span>❤️</span><span>{heart}</span></reaction>
		{rocket:} <reaction><span>🚀</span><span>{rocket}</span></reaction>
		{eyes:} <reaction><span>👀</span><span>{eyes}</span></reaction>
	</p>
{/reactions}
